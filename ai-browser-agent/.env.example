# Public extension setting: the local or deployed SchemeSathi backend endpoint.
# Do not add OpenAI, Gemini, JWT, or other secrets here; extension build variables
# are bundled into the extension and can be read by users.
VITE_AGENT_BACKEND_URL=http://localhost:3000/api/browser-agent/plan
